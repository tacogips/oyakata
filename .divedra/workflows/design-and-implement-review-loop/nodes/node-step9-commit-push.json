{
  "id": "step9-commit-push",
  "nodeType": "command",
  "variables": {},
  "command": {
    "scriptPath": "scripts/commit-and-push.sh",
    "envTemplate": {
      "COMMIT_MESSAGE": "{{inbox.latest.output.commitMessage}}",
      "WORKFLOW_MODE": "{{inbox.latest.output.workflowMode}}"
    }
  },
  "output": {
    "description": "Return JSON with the commit hash, pushed remote and branch, push status, and the commit message used."
  }
}
